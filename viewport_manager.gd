class_name SViewportManager extends Node

# var emu_con := EmulatorContainer.new()

func _init() -> void:
	pass
	# self.emu_con.int_con.base_size = Vector2i(ProjectSettings.get_setting(&"display/window/size/viewport_width", 427) as int, ProjectSettings.get_setting(&"display/window/size/viewport_height", 320) as int)
	#
	# self.emu_con.bg.texture = load(&"res://gui/theme/square.png")
	# self.emu_con.pp_display.chain = load(&"res://shaders/post_processing_chain_display.tres")

# func _ready() -> void:
	# self.get_parent().add_child.call_deferred(self.emu_con)
